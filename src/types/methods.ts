export enum HTTPMethods {
  Get = 'GET',
  Post = 'POST',
  Delete = 'DELETE',
  Put = 'PUT',
  Options = 'OPTIONS',
  //add Options - because of not simple request for application/json type use thi method
}
